# DEPRECATED — Plugin config has moved to .mcp-me.yaml
# This file is kept for backward compatibility only.
# Please use .mcp-me.yaml for all configuration (generators + plugins).
plugins:
  # GitHub — live repos, contributions, and activity
  # github:
  #   enabled: true
  #   username: "your-github-username"
  #   token_env: "GITHUB_TOKEN"     # optional, for higher rate limits

  # Spotify — now playing, top artists, recently played (requires OAuth)
  # spotify:
  #   enabled: true
  #   client_id_env: "SPOTIFY_CLIENT_ID"
  #   client_secret_env: "SPOTIFY_CLIENT_SECRET"
  #   refresh_token_env: "SPOTIFY_REFRESH_TOKEN"

  # LinkedIn — professional history from exported data
  # linkedin:
  #   enabled: true
  #   data_path: "./linkedin-export.json"

  # WakaTime — live coding activity, today's stats, weekly summary
  # wakatime:
  #   enabled: true
  #   username: "your-wakatime-username"
  #   api_key_env: "WAKATIME_API_KEY"  # optional, for private profiles

  # DEV.to — live article stats, latest posts
  # devto:
  #   enabled: true
  #   username: "your-devto-username"

  # Bluesky — live feed, profile, follower stats (no auth needed)
  # bluesky:
  #   enabled: true
  #   handle: "you.bsky.social"

  # Hacker News — live karma, recent submissions (no auth needed)
  # hackernews:
  #   enabled: true
  #   username: "your-hn-username"

  # Reddit — live karma, recent posts (no auth needed)
  # reddit:
  #   enabled: true
  #   username: "your-reddit-username"
